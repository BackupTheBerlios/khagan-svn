To install Khagan, just type:

  python setup.py install

This will install Khagan in the same directory as you have Python
installed in. On most Unix systems this means /usr. If you want 
to install somewhere else type
  python setup.py install --prefix DIRECTORY

